const initialstate={

}
export const reducer =(state=initialstate,action)=>{
    switch(action.type){
      // create your own case:
          
        default:
         return state
    }
  }
